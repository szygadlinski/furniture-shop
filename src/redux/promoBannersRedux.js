/* selectors */
export const getAllBanners = ({ promoBanners }) => promoBanners;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
