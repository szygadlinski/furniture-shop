import React from 'react';
// import PropTypes from 'prop-types';
import styles from './Account.module.scss';

const Account = () => <div className={styles.root}>This is Account</div>;

// Account.propTypes = {};

export default Account;
